import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const TestTwo = (props) => {
	return (
		<Fragment>
			<div>this is test two page </div>
		</Fragment>
	);
};

TestTwo.propTypes = {};

export default TestTwo;
